$("#user-<%= @invitation.to_user_id %>").find('.invitation-button').attr('class', 'btn btn-success invitation-button disabled');
$("#user-<%= @invitation.to_user_id %>").find('.invitation-button').attr('value', '预约成功');